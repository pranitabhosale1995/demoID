import React from "react";
import { Grid, Typography, Button, Box } from "@mui/material";
import {
  RootContainer,
  ContentGrid,
  GlassCard,
  StyledTextField,
} from "./LoginStyles";
import fincoreLogo from "../../../assets/Logos/logosss.svg";

const Login = () => {
  const infoPoints = [
    "End-to-end 256-bit SSL encryption",
    "Role-based Multi-factor access control",
    "Real-time tracking and auditing",
  ];

  return (
    <RootContainer>
      <ContentGrid container alignItems="center" justifyContent="space-between" spacing={5}>
        {/* LEFT INFO SECTION */}
        <Grid item xs={12} md={6}>
          <Box>
            <Box display="flex" alignItems="center" mb={3}>
              <img src={fincoreLogo} alt="FinCore" width={90} height={90} />
              <Box ml={2}>
                <Typography variant="h4" color="primary.main" fontWeight={700}>
                  FinCore
                </Typography>
                <Typography variant="body2" color="text.secondary">
                  Empowering Data
                </Typography>
              </Box>
            </Box>

            <Typography
              variant="h5"
              color="text.primary"
              fontWeight={700}
              mb={2}
            >
              Welcome to FinCore Reporting Portal
            </Typography>

            <Typography variant="body1" color="text.secondary" mb={3}>
              Seamlessly access and generate critical financial reports with
              FinCore – SBI's centralized platform for secure, high integrity
              financial data access. Designed for internal operations only.
            </Typography>

            <Box mt={4}>
              {infoPoints.map((text, i) => (
                <Box display="flex" alignItems="center" gap={1.5} mb={1.5} key={i}>
                  <Box
                    sx={{
                      width: 18,
                      height: 18,
                      borderRadius: "50%",
                      bgcolor: "secondary.main",
                    }}
                  />
                  <Typography variant="body2" color="text.primary">
                    {text}
                  </Typography>
                </Box>
              ))}
            </Box>
          </Box>
        </Grid>

        {/* RIGHT LOGIN FORM */}
        <Grid item xs={12} md={5}>
          <GlassCard elevation={0}>
            <Typography
              variant="h6"
              textAlign="center"
              color="text.primary"
              fontWeight={700}
              mb={3}
            >
              Sign In
            </Typography>

            <form>
              <StyledTextField label="User ID" fullWidth size="small" />
              <StyledTextField
                label="Password"
                type="password"
                fullWidth
                size="small"
              />
              <Button
                type="submit"
                fullWidth
                variant="contained"
                sx={{
                  py: 1,
                  mt: 2,
                  borderRadius: 2,
                  fontWeight: 600,
                  textTransform: "none",
                  background:
                    "linear-gradient(90deg, #5D3FD3 0%, #A28DF0 100%)",
                }}
              >
                SIGN IN
              </Button>
            </form>
          </GlassCard>
        </Grid>
      </ContentGrid>
    </RootContainer>
  );
};

export default Login;
