// src/features/auth/components/LoginPort.jsx
import React from "react";
import { Box, Typography, TextField, Button, Avatar } from "@mui/material";
import fincoreLogo from "../../../assets/Logos/logosss.svg";
import {
  RootContainer,
  ContentWrapper,
  LeftColumn,
  RightColumn,
  GlassCard,
} from "./LoginStyles";

const LoginPort = () => {
  const bullets = [
    "End-to-end 256-bit SSL encryption",
    "Role-based Multi-factor access control",
    "Real-time tracking and auditing",
  ];

  return (
    <RootContainer>
      <ContentWrapper>
        <LeftColumn>
          <Box display="flex" alignItems="center" gap={2} mb={3}>
            <Avatar src={fincoreLogo} alt="FinCore" sx={{ width: 68, height: 68, bgcolor: "transparent" }} />
            <Box>
              <Typography variant="h6" fontWeight={700} color="text.primary">FinCore</Typography>
              <Typography variant="body2" color="text.secondary">Empowering Data</Typography>
            </Box>
          </Box>

          <Typography variant="h4" fontWeight={700} mb={2} color="text.primary">
            Welcome to FinCore Reporting Portal
          </Typography>

          <Typography variant="body1" color="text.secondary" mb={4}>
            Seamlessly access and generate critical financial reports with FinCore — SBI's centralized platform for secure, high integrity financial data access. Designed for internal operations only.
          </Typography>

          {bullets.map((b, i) => (
            <Box key={i} display="flex" alignItems="center" gap={2} mb={2}>
              <Box sx={{ width: 18, height: 18, borderRadius: "50%", bgcolor: "primary.main" }} />
              <Typography variant="body1" color="text.primary">{b}</Typography>
            </Box>
          ))}
        </LeftColumn>

        <RightColumn>
          <GlassCard elevation={3}>
            <Typography align="center" variant="h6" mb={2} fontWeight={700} color="text.primary">
              Sign In
            </Typography>

            <Box component="form" noValidate>
              <TextField label="User ID" variant="outlined" fullWidth size="small" margin="normal" />
              <TextField label="Password" variant="outlined" type="password" fullWidth size="small" margin="normal" />
              <Button fullWidth variant="contained" sx={{ mt: 2, py: 1.2, borderRadius: 2 }}>
                SIGN IN
              </Button>
            </Box>
          </GlassCard>
        </RightColumn>
      </ContentWrapper>
    </RootContainer>
  );
};

export default LoginPort;
